// Copy this file to config.js if you need to point the frontend at a backend
// running on a different origin than the frontend itself.
// Example: API_BASE_URL: "https://farmora-backend.example.com"
window.FARMORA_CONFIG = {
  API_BASE_URL: ""
};
